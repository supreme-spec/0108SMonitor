{
  "version": "v1",
  "hooks": [
    {
      "name": "Run Prisma Migration",
      "trigger": "PreToolUse",
      "description": "Применить миграцию Prisma для новых моделей архива",
      "action": {
        "type": "command",
        "command": "npx prisma migrate dev --name add_archive_models"
      }
    }
  ]
}